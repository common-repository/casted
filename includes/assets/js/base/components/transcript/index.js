/**
 * Transcript component
 */

const Transcript = (props) => {
	const { transcript } = props;

	return <div className="casted-transcript">Transcript will go here</div>;
};

export default Transcript;
