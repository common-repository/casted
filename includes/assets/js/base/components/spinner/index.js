/**
 * External Dependencies
 */
import { Spinner } from '@wordpress/components';

const CastedSpinner = () => {
	return (
		<div className="casted-spinner">
			<Spinner />
		</div>
	);
};

export default CastedSpinner;
